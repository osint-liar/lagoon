 [
    {
        "title": "Calendar Heat Map By Hours",
        "id": "calendar-heat-map-hours",
        "active": true,
        "route": "/timelines/calendar-heat-map-hours",
        "description": "Calendar Heat Map By Hours",
        "sql": "SELECT strftime('%s', strftime('%Y-%m-%d', ContentStartedOn, 'utc')) AS Day, COUNT(*) AS Count FROM ApiCoreData WHERE ContentStartedOn IS NOT NULL AND CaseManagementUuid = :CaseManagementUuid GROUP BY Day ORDER BY Day",
        "dataUrl": "{{WebHost}}v1/analysis-tool/raw-sql?SqlQuery={{SqlQuery}}&CaseManagementUuid={{CaseManagementUuid}}",
        "specification": {

        }
    }
 ]